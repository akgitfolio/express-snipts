const express = require('express');
const app = express();

// Define a route for API version 1
app.get('/api/v1/', (req, res) => {
  // Handle API version 1 requests
});

// Define a route for API version 2
app.get('/api/v2/', (req, res) => {
  // Handle API version 2 requests
});
